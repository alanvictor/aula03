import { ToggleNavigationDirective } from './toggle-navigation.directive';

describe('ToggleNavigationDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ToggleNavigationDirective();
  //   expect(directive).toBeTruthy();
  // });
});
